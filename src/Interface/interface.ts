export type SignUpType = {
  name: string;
  email: string;
  password: string;
  confirmpassword: string;
};
export type SignInType = {
  email: string;
  password: string;
};

export type Otp = {

  otp:'',
}